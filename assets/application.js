// Put your application javascript here
jQuery(document).ready(() => {
    jQuery('.hero-slider__wrapper').slick({
      prevArrow: '.hero-slider__button--prev',
      nextArrow: '.hero-slider__button--next'
    })
});